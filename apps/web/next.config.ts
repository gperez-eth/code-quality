import type { NextConfig } from 'next';

// Intentionally empty, and meant to stay that way: anything heavy reappearing
// here means something has been pulled back into the request path. Vault note:
// `Packages` — "The dependency rule that matters".
const config: NextConfig = {};

export default config;
